# This is used as static fallback.
# In production, frpc-entrypoint.sh generates the config dynamically.
serverAddr = "127.0.0.1"
serverPort = 7002
auth.token = "chator-frp-secret"
transport.tls.enable = false
transport.protocol = "tcp"
loginFailExit = false

[[proxies]]
name = "web"
type = "tcp"
localIP = "caddy"
localPort = 80
remotePort = 8080
